# Loved
an easy cross-platform love building script made in python that needs to be made

## Dependencies
* Requests (the python module)
  * `python -m pip install requests`
* Shutil (the python module)
    * `python -m pip install shutil`
